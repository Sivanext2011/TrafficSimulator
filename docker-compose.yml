version: '3.8'

services:
  simulator:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./certs:/app/certs
      - ./logs:/app/logs
      - ./config:/app/config
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped
